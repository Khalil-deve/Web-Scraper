/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  images: {
    domains: ['m.media-amazon.com', 'ia.media-imdb.com', 'www.rottentomatoes.com', 'www.themoviedb.org'],
  },
};

export default nextConfig;
